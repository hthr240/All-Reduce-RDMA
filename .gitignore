# Keep the repository limited to C sources, headers, and the Makefile;
# benchmark results and the report join here once written.
*
!.gitignore
!*.c
!*.h
!Makefile
!results-*.tsv
!tests/
tests/*
!tests/*.c
